var payRate
var hoursWorked
var overRate
payRate = 25.00
hoursWorked = 40


if (hoursWorked == 40 ){
 grossPay = hoursWorked * payRate
console.log(grossPay) }
else if {
overRate = payRate * 1.5
console.log(overRate)
}
